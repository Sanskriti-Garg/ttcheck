import { MultiSelect } from 'primereact/multiselect';

// ... other imports and code ...

<div className="four wide field pb-10">
  <label
    className="form-label fw-semibold"
    style={{ fontSize: "15pt" }}
  >
    Assessment Status<span className="required">*</span>
  </label>
  <br />
  <MultiSelect
    style={{ width: "30rem" }}
    placeholder="Assessment Status"
    value={vrmData.vrmAssessmentDatacopy.assessmentStatus || []}
    options={assessmentStatusMasterData.filter(option => option.value !== -1)} // exclude the "Select" option
    onChange={(e) => {
      dispatch(setAssessmentStatus(e.value));
      validateAssessmentStatus(e.value);
    }}
    id="assessmentstatus"
    name="assessmentstatus"
    aria-describedby="title-error"
    required
    aria-invalid={!!error}
    display="chip"
    selectedItemsLabel="{0} selected"
    maxSelectedLabels={1} // Show only 1 item, then "+X selected"
    showSelectAll={false}
  />
</div>
